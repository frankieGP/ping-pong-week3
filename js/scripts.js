$(document).ready(function() {
  $("form#pingpongn").submit(function(event) {
  event.preventDefault();
  var enteredNumber = $("input#enterNumber").val();

  var hereWeGo = "";

  var numberList = (enteredNumber);
for (var i = 1; i <= numberList; i++) {

  return answers.empty();
}
    });
  });
